class UserInfo {
    construtor(project, firstName, lastName, cohort, program, username) {
        this.project = project;
        this.firstName = firstName;
        this.lastName = lastName;
        this.cohort = cohort;
        this.program = program;
        this.username = username;
    }
}

module.exports = UserInfo;